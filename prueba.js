const { guardarDB, crearTarea, cargarDB, borrarTarea, actualizarEstado } = require('./tareas')
//console.log(crearTarea("tarea Numero 1", "creo una descripcion de lo que quiero hacer"))
//console.log(cargarDB())
//console.log(crearTarea("tarea Numero 2", "desc 2"))
//borrarTarea("tarea Numero 1")
//actualizarEstado("tarea Numero 2", false)

/* const yargs = require('yargs/yargs')
const { hideBin } = require('yargs/helpers')
const argv = yargs(hideBin(process.argv)).argv

if (argv.ships > 3 && argv.distance < 53.5) {
    console.log('Plunder more riffiwobbles!')
} else {
    console.log('Retreat from the xupptumblers!')
} */